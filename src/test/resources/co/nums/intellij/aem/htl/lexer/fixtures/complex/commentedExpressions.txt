<!--/* ('<!--/*')
COMMENT_CONTENT ('${this should be HTL comment}')
*/--> ('*/-->')
OUTER_TEXT ('\n')
${ ('${')
SINGLE_QUOTED_STRING (''this should be parsed as expression'')
} ('}')
OUTER_TEXT ('\n<!--')
${ ('${')
SINGLE_QUOTED_STRING (''this should be parsed as expression because it is HTML (not HTL) comment'')
} ('}')
OUTER_TEXT ('-->\n')
${ ('${')
SINGLE_QUOTED_STRING (''this should be parsed as expression'')
} ('}')
